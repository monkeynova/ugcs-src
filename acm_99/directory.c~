#include <stdio.h>

struct listing {
  char name[53];
  char number[7];
  char sortName[ 150 ];
};

void main( void ) {
  listing everyone[ 1000 ];
  char s[80]

  while( ! feof( stdin ) ) {
    if ( fgets( s, 80, stdin ) == NULL )
      break;

  }
}
